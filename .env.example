# akiwaky.cloud — environment variables
# Copy to .env.local for local dev. .env.local is gitignored.

# (No runtime env required for the current static homepage.)
# Reserved for future use, e.g. contact form webhook:
# NEXT_PUBLIC_CONTACT_WEBHOOK=
